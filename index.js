// Write your code here!\
const element = document.getElementById("main");
element.remove(element);

const newHeader = document.createElement("h1");

newHeader.id = ("victory");

newHeader.innerHTML = "DOROTHY is the champion";